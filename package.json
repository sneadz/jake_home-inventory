{
  "name": "inventory-app",
  "private": true,
  "workspaces": ["api", "web"],
  "scripts": {
    "dev": "npm run dev -w api",
    "build": "npm run build -w web && npm run build -w api",
    "start": "npm run start -w api"
  }
}
